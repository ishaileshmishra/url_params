name: url_params
description: This package helps to add query parameters to a Dart that appends the '?param1=one&param2=two' to the url.
version: 0.0.2
homepage: https://github.com/ishaileshmishra/url_params

environment:
  sdk: '>=2.7.0 <3.0.0'

#dependencies:
#  path: ^1.6.0

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.6.0
